{
  "name": "service-alphavantage",
  "version": "1.0.0",
  "description": "Fetches stock quotes from API",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "NODE_ENV=dev node index.js",
    "deploy": "now -e API_KEY=@alphavantage-api-key -e JWT_SECRET=@jwt-secret",
    "start": "NODE_ENV=production node index.js"
  },
  "author": "Ben Ipsen <email@benipsen.com> (http://www.benipsen.com)",
  "license": "N/A",
  "dependencies": {
    "dotenv": "^4.0.0",
    "express": "^4.16.2",
    "express-jwt": "^5.3.0"
  }
}
